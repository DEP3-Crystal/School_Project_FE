import { DepartmentWithoutRef } from "./department-without-ref";
import { RoomWithoutRef } from "./room-without-ref";

export class SchoolWithoutRef {
    schoolId!: number;
    location!: string;
    name!: string;
  }